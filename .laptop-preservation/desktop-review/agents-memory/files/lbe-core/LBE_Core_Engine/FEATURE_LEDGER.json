{
  "version": 1,
  "features": [
    {
      "id": "local-sdk-cli",
      "state": "ACTIVE",
      "description": "Local SDK and CLI execution boundary.",
      "allowedPaths": ["src/core/**", "src/cli/**", "src/hooks/**", "docs/**"],
      "requiredProof": ["npm test", "npm pack --dry-run"]
    },
    {
      "id": "execution-bridge",
      "state": "ACTIVE",
      "description": "Agent actions must route through LBE and create audit/observe entries.",
      "allowedPaths": ["src/hooks/**", "src/cli/**", ".lbe/**"],
      "requiredProof": [".lbe/observe.jsonl", ".lbe/audit.jsonl"]
    },
    {
      "id": "mcp-server",
      "state": "PLANNED",
      "description": "Optional MCP tool surface. Not enforcement unless native tools are disabled.",
      "allowedOnlyIfIssueExists": true
    },
    {
      "id": "dashboard",
      "state": "BLOCKED",
      "description": "Dashboard is blocked for current release."
    },
    {
      "id": "hosted-service",
      "state": "BLOCKED",
      "description": "Hosted service is blocked for solo-dev local-first release."
    },
    {
      "id": "cep-specific-adapter",
      "state": "DEPRECATED",
      "description": "CEP-specific code does not belong in the core SDK package unless split into adapter package."
    }
  ]
}
